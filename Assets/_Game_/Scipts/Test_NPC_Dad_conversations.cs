using UnityEngine;

public class Test_NPC_Dad_conversations : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    // sletter det ti sidst, bare for at teste at det virkede
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
